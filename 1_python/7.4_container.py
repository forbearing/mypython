=== 2020-6-2
1:把多个元素组织在一起的数据结构，其元素可以逐个迭代获取，可以用 in not in 关键字
  判断是否包含在容器中。
2:通常这类数据结构把所有的元素存储在内存中。也有一些特例，并不是所有的元素都存放在内存
  比如迭代器和生成器对象。
3:常见迭代器对象有
    str
    tuple  namedtuple
    list  deque
    set  frozensets
    dict  defaultdict  OrdereedDict  Counter
