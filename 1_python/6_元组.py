概念
    1:Python 的元祖与列表类似，不同之处在于元祖的元素不能修改
    2:元祖使用小括号，列表使用大括号
    3:元祖创建很简单，只需在括号中添加元素，并使用逗号隔开

元祖运算符
    len((1,2,3))
    (1,2,3) + (3,4,5)
    ('Hi') * 4
    3 in (1,2,3)
    for x in(1,2,3); print(x,)

元祖内置函数
    len(tuple)          计算元组元素个数
    max(tuple)          返回元组中元素最大值
    min(tuple)          返回元组中元组最小值
    tuple(iterable)     将可迭代系列转换为元组
