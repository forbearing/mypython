1:概述
    1:表达式：由变量、常量 、运算符组成的式子。


算数运算符 + - * /
关系运算符 == != > < >= <=
逻辑运算符 and or not
位运算符
    &       按位与，都为1结果位1，否则位0
    |       按位或，两个二进制位有一个位为1时，结果为1
    ^       按位异或，二进制的两位想异时，结果为1
    ~       按位取反，每个二进制位取反，1变0，0变1
    <<      左移动运算符，二进制位全部左移动若干位
    >>      右移动运算符，二进制位全部右移动若干位
成员运算符
    in      如果在指定的序列中找到了值返回 True，否则返回 False
    not in  如果在指定个的序列中没有找到值，返回 True
身份运算符
    is      判断两个标志符是否引用同一个对象
    is not  判断两个标志符是否引用不同的对象


运算符优先级
